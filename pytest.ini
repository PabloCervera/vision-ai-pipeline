[pytest]
# Añade src/ al sys.path para que los tests importen igual que la aplicación
# (from config import ..., from detection... import ..., etc.).
pythonpath = src
testpaths = tests
asyncio_default_fixture_loop_scope = function
